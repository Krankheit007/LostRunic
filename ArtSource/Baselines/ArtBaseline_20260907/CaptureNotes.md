# ArtBaseline_20260907 capture notes

This directory is the P0 render baseline using the user-requested identifier `ArtBaseline_20260907`. The actual capture and file sealing date is **2026-09-08** (Asia/Shanghai), recorded in every JSON snapshot and in `Manifest.json`.

The Unreal Editor was left on `/Game/LostRunic/Levels/PIE_Test/L_Art_Demo`. Capture used the local Unreal MCP endpoint at `127.0.0.1:8000/mcp` with tool schemas read first and read-only inspection calls. No level, Blueprint, DataAsset, light, post-process, material or texture was edited by the capture. A temporary write probe was removed before sealing.

The confirmed camera contract is distance 500 cm, pitch -52 degrees and FOV 68 degrees. The authored BP values and a second PIE read were both captured. Before the user changed `DA_LRPresentationTuning.DefaultCameraDistanceCm`, the first PIE read observed the runtime boom at 700 cm. After the user change, a second PIE read observed DA, CameraRig and runtime boom at 500 cm with FOV 68. This is the effective current result; the earlier 700 observation remains historical evidence and is not presented as current. The editor viewport screenshot is explicitly an `EditorViewport` image at reported FOV 90, so it is visual evidence only and is not a Gameplay/PIE screenshot.

`LightingAndPostProcess.json` contains the named 14 light actors and both existing PPV actors. It records light transforms, bounded render parameters, PPV priority/blend settings, selected post-process override flags, selected values and weighted blendables. `MaterialInstances.json` records the three active outline MIs and their parameters. Their serialized blendable location fields say `AfterTonemapping`, but the read `bOverrideBlendableLocation=false`; the parent materials report effective `AfterDOF` (interaction parent priority 10, style parent priority 0). The inactive serialized values are retained as raw evidence and must not be treated as the effective location.

`SceneMaterialCoverage.json` records five representative living-room Scene MIs and their direct parent, serialized values, texture references and representative mesh slot assignments. It is a bounded sample. Exhaustive level-wide geometry and all 32 Scene MI values were not completed and are marked `NOT_CAPTURED`.

The actual current binary copies needed for the next outline work are under `WorkspaceOverrides/Content/LostRunic/Materials/`: the three outline MIs, both outline parent materials, four outline functions and two parameter collections. The user-confirmed BP, map and newly edited presentation DataAsset copies are at `WorkspaceOverrides/BP_Ruth.uasset`, `WorkspaceOverrides/L_Art_Demo.umap` and `WorkspaceOverrides/DA_LRPresentationTuning.uasset`. Existing texture assets were left untouched; texture references and hashes are recorded where inspected, while actual texture blob backups are `NOT_CAPTURED`.

`Environment.json` records UE 5.8.2 CL 56702186, D3D12/SM6, NVIDIA GeForce RTX 4080 SUPER driver 591.86 from the UE startup log, Git HEAD/branch and the bounded worktree status. Windows WMI GPU/OS queries were denied and are recorded as `NOT_CAPTURED`; the UE log supplied the OS/RHI/GPU evidence. `RenderingCVars.json` stores only the explicit Lumen, virtual-shadow query, AA/TSR, screen-percentage, exposure, fog and quality whitelist. It does not copy `DefaultEngine.ini` or unrelated credentials.

The detached manifest hash and `Verify-Baseline.ps1` provide immutable-file verification. `Diff-Preview.ps1` compares only manifest entries with an explicitly listed workspace target. `Restore-Preview.ps1` prints limited manual copy commands and never applies them. Any restoration must be manually selected, target-specific and performed with the editor closed; no automatic Unreal state restoration is provided.

Known missing evidence: clean Gameplay/PIE screenshot, GPU visualizer/profile, final runtime blended PP stack, exhaustive 81-geometry/32-MI enumeration, transitive parent graph beyond direct dependency reads, and actual texture blob backups. No restoration, full build, full test suite or GPU profile pass is claimed by this baseline.
